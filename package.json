{
  "name": "single-threaded",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "biome": "biome check --write .",
    "build": "tailwindcss -i styles.src.css -o public/styles.css --minify"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.6",
    "@tailwindcss/cli": "^4.3.3"
  }
}
